import actions from './action-types'
import mutations from './mutation-types'

const state = {
  imageUrl: '',
  beforeCrawlingUrl: ''
}

const getters = {}

export default {
  state,
  getters,
  actions,
  mutations,
}
